NODE_ENV: development
# HTTP
PORT: 3000
# Postgresql
DB_HOST: localhost
DB_PORT: 5432
DB_USER: express_demo
DB_PASSWORD: expressDemo2024
DB_NAME: express_demo

# Secret path: just simple demonstration about the function, not to describe real production usage

# directory should be owned by user who run the project under node env
KMS_SECRET_PATH: /data/kms-dev
USER_SECRET_FILE: /data/kms-dev/user.secret
KMS_TOKEN_EXPIRES_MINUTES: 1440
AUTH_TOKEN_EXPIRE_MINUTES: 1440